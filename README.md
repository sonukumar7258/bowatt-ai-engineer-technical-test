# BoWatt Applied AI Engineer Technical Test

This repository contains the implementation for the BoWatt Applied AI Engineer Technical Test.

## Repository status

The initial commit preserves the provided React frontend unchanged. The backend implementation will be added in the required `rest/` directory in subsequent, focused commits.

## Provided frontend

The frontend is a Vite, React, and TypeScript application. It provides a research-request input, source-file uploads, and streamed Markdown response rendering.
